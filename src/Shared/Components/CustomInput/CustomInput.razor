<input type="text" @onchange=@ChangeText class="component" />

@code {
    [Parameter]
    public EventCallback<string> OnChange { get; set; }

    async Task ChangeText(ChangeEventArgs ev) {

        await OnChange.InvokeAsync(ev.Value.ToString());
    }
}
